#!/bin/bash
# Real-time upload monitor with beautiful UI

LOG="/Users/alec/.cursor/projects/Users-alec-salfagpt/terminals/3.txt"

while true; do
  clear
  echo "╔════════════════════════════════════════════════════════════════════════╗"
  echo "║           🚀 M3-V2 PARALLEL UPLOAD - LIVE MONITOR                      ║"
  echo "╚════════════════════════════════════════════════════════════════════════╝"
  echo ""
  
  # Check if process is running
  if ps aux | grep -E "upload.ts.*M3-v2" | grep -v grep > /dev/null; then
    echo "✅ Status: RUNNING"
  else
    echo "⏹️  Status: COMPLETED or STOPPED"
  fi
  
  echo ""
  echo "────────────────────────────────────────────────────────────────────────"
  echo "📊 PROGRESS"
  echo "────────────────────────────────────────────────────────────────────────"
  
  # Parse progress from log
  STARTED=$(grep -c "📄 ARCHIVO" "$LOG" 2>/dev/null || echo "0")
  COMPLETED=$(grep -c "✅ ARCHIVO COMPLETADO\|✅ Documento asignado" "$LOG" 2>/dev/null || echo "0")
  FAILED=$(grep -c "\[.*FAILED:" "$LOG" 2>/dev/null || echo "0")
  
  echo "Files: $COMPLETED completed, $FAILED failed, $((62 - COMPLETED - FAILED)) pending"
  
  # Progress bar
  if [ "$STARTED" -gt 0 ]; then
    PERCENT=$((COMPLETED * 100 / 62))
    FILLED=$((PERCENT / 2))
    EMPTY=$((50 - FILLED))
    printf "["
    printf "%${FILLED}s" | tr ' ' '█'
    printf "%${EMPTY}s" | tr ' ' '░'
    printf "] $PERCENT%% ($COMPLETED/62)\n"
  fi
  
  echo ""
  echo "────────────────────────────────────────────────────────────────────────"
  echo "📄 RECENT ACTIVITY (Last 30 lines)"
  echo "────────────────────────────────────────────────────────────────────────"
  
  tail -30 "$LOG" 2>/dev/null | grep -E "📄|✅|❌|🤖|💾|🧬|📊|🔗" | tail -20
  
  echo ""
  echo "────────────────────────────────────────────────────────────────────────"
  echo "Press Ctrl+C to exit monitor | Updates every 5s"
  echo "────────────────────────────────────────────────────────────────────────"
  
  sleep 5
done
