---
import Layout from '../layouts/Layout.astro';
import AdminPanel from '../components/AdminPanel';

const currentUserId = 'alec_getaifactory_com'; // This should come from auth session
---

<Layout title="Administración - Flow">
  <AdminPanel client:load currentUserId={currentUserId} />
</Layout>

