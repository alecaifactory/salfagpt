---
import { getSession } from '../lib/auth';
import '../styles/global.css';

// Prevent caching to avoid serving stale HTML with old CSS references
Astro.response.headers.set('Cache-Control', 'no-cache, no-store, must-revalidate');
Astro.response.headers.set('Pragma', 'no-cache');
Astro.response.headers.set('Expires', '0');

// Redirect to chat if already logged in
const session = getSession(Astro);
if (session) {
  return Astro.redirect('/chat');
}

// Get error and success parameters
const errorParam = Astro.url.searchParams.get('error');
const logoutParam = Astro.url.searchParams.get('logout');

// Error messages for OAuth failures
const errorMessages: Record<string, { title: string; message: string }> = {
  'auth_failed': {
    title: 'Error de Autenticación',
    message: 'No se pudo completar el inicio de sesión. Intenta nuevamente.'
  },
  'no_code': {
    title: 'Error de Autorización',
    message: 'No se recibió el código de autorización de Google.'
  },
  'auth_processing_failed': {
    title: 'Error al Procesar',
    message: 'Ocurrió un error al procesar tu inicio de sesión.'
  }
};

const currentError = errorParam ? errorMessages[errorParam] : null;
const showLogoutSuccess = logoutParam === 'success';
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="google" content="notranslate">
  <title>SalfaGPT - AI-Powered Conversations</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body class="min-h-screen">
  <!-- Hero Section with Split Layout -->
  <div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-50 via-blue-50 to-slate-50 px-4 py-12">
    <div class="max-w-6xl w-full">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        
        <!-- Left Side: Message/Value Proposition -->
        <div class="space-y-6">
          <!-- Salfacorp Logo -->
          <div class="flex justify-center md:justify-start">
            <img 
              src="/images/Logo Salfacorp.png" 
              alt="Salfacorp" 
              class="w-32 h-32 md:w-40 md:h-40 object-contain"
            />
          </div>
          
          <div class="space-y-4">
            <h1 class="text-4xl md:text-5xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
              ¡Hola! Soy tu agente SalfaCorp
            </h1>
          </div>
          
          <div class="space-y-4 text-slate-700 text-base md:text-lg leading-relaxed">
            <p>
              En SalfaCorp estamos comprometidos con la innovación y la transformación digital, integrando herramientas de inteligencia artificial para elevar tu eficiencia y productividad. Estos agentes están diseñados para ser tus aliados en procesos y para responder todas tus consultas a cualquier hora y desde cualquier dispositivo.
            </p>
            
            <p class="text-slate-600 text-sm md:text-base">
              Ten en cuenta que este agente se basa en inteligencia artificial, por lo que sus respuestas se generan en función de probabilidades. Si no estás de acuerdo con las respuestas puedes escribirnos a <a href="mailto:soporteia@salfagestion.cl" class="text-blue-600 hover:text-blue-700 underline font-medium">soporteia@salfagestion.cl</a>
            </p>
            
            <p class="text-slate-800 font-semibold">
              Inicia sesión con tu correo corporativo y transforma tu día a día. Únete ahora y haz que cada momento cuente, mientras evolucionamos juntos el futuro con soluciones inteligentes.
            </p>
          </div>
        </div>

        <!-- Right Side: Login Card -->
        <div class="flex items-center justify-center">
          <div class="bg-white rounded-2xl shadow-2xl p-8 md:p-12 w-full max-w-md border border-slate-200">
            <div class="space-y-6">
              <div class="text-center space-y-2">
                <h2 class="text-3xl font-bold text-slate-800">Bienvenido</h2>
                <p class="text-slate-600">Inicia sesión para continuar</p>
              </div>

              <!-- Success Message (Logout) -->
              {showLogoutSuccess && (
                <div class="bg-green-50 border border-green-200 rounded-lg p-4">
                  <div class="flex items-start gap-3">
                    <svg class="w-6 h-6 text-green-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div>
                      <h3 class="text-green-800 font-semibold mb-1">Sesión Cerrada</h3>
                      <p class="text-green-700 text-sm">Has cerrado sesión exitosamente.</p>
                    </div>
                  </div>
                </div>
              )}

              <!-- Error Message -->
              {currentError && (
                <div class="bg-red-50 border border-red-200 rounded-lg p-4">
                  <div class="flex items-start gap-3">
                    <svg class="w-6 h-6 text-red-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div>
                      <h3 class="text-red-800 font-semibold mb-1">{currentError.title}</h3>
                      <p class="text-red-700 text-sm">{currentError.message}</p>
                    </div>
                  </div>
                </div>
              )}
              
              <!-- Google Sign-In Button -->
              <div class="space-y-4">
                <a 
                  href="/auth/login-redirect"
                  class="w-full flex items-center justify-center space-x-3 bg-white hover:bg-gray-50 text-gray-900 font-semibold py-4 px-6 rounded-xl transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
                >
                  <svg class="w-6 h-6" viewBox="0 0 24 24">
                    <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                    <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                    <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                    <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                  </svg>
                  <span>Continuar con Google</span>
                </a>
                
                <div class="text-center">
                  <p class="text-sm text-slate-500">
                    Al continuar, aceptas nuestros Términos de Servicio y Política de Privacidad
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
        
      </div>
    </div>
  </div>
</body>
</html>
