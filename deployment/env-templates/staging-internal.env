# Staging Internal Environment (Your GCP)
# Purpose: Internal QA and validation before client deployment
# Project: gen-lang-client-0986191192

NODE_ENV=staging
ENVIRONMENT_NAME=staging-internal

# GCP Configuration (SAME as current - backward compatible)
GOOGLE_CLOUD_PROJECT=gen-lang-client-0986191192
REGION=us-central1

# OAuth Configuration (Create separate OAuth client for staging)
# REQUIRED: Create new OAuth client in console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=[CREATE-NEW-STAGING-OAUTH-CLIENT-ID]
GOOGLE_CLIENT_SECRET=[CREATE-NEW-STAGING-OAUTH-CLIENT-SECRET]

# Gemini AI (can reuse existing key)
GOOGLE_AI_API_KEY=[YOUR-EXISTING-GEMINI-API-KEY]

# JWT Secret (generate new for staging: openssl rand -base64 32)
JWT_SECRET=[GENERATE-NEW-JWT-SECRET]

# Service URL (update after first deployment)
PUBLIC_BASE_URL=https://flow-staging-internal-xxx.run.app

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_DEBUGGING=true
ENABLE_RATE_LIMITING=false







