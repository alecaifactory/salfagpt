# Client Staging Environment (Client GCP UAT)
# Purpose: Client user acceptance testing
# Project: [CLIENT-STAGING-PROJECT-ID]

NODE_ENV=staging
ENVIRONMENT_NAME=staging-client

# GCP Configuration (REPLACE WITH CLIENT VALUES)
GOOGLE_CLOUD_PROJECT=[CLIENT-STAGING-PROJECT-ID]
REGION=us-central1

# OAuth Configuration (Client-specific, create in their project)
# REQUIRED: Create OAuth client in client's GCP project
GOOGLE_CLIENT_ID=[CLIENT-STAGING-OAUTH-CLIENT-ID]
GOOGLE_CLIENT_SECRET=[CLIENT-STAGING-OAUTH-CLIENT-SECRET]

# Gemini AI (Client's key or yours)
GOOGLE_AI_API_KEY=[CLIENT-OR-YOUR-GEMINI-API-KEY]

# JWT Secret (unique for client staging: openssl rand -base64 32)
JWT_SECRET=[GENERATE-NEW-JWT-SECRET-FOR-CLIENT-STAGING]

# Service URL (update after first deployment)
PUBLIC_BASE_URL=https://staging.client-domain.com

# Custom Domain (optional)
CUSTOM_DOMAIN=staging.client-domain.com

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_DEBUGGING=true
ENABLE_RATE_LIMITING=true










