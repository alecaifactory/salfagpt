# Client Production Environment (Client GCP Live)
# Purpose: Customer-facing production service
# Project: [CLIENT-PRODUCTION-PROJECT-ID]

NODE_ENV=production
ENVIRONMENT_NAME=production-client

# GCP Configuration (REPLACE WITH CLIENT VALUES)
GOOGLE_CLOUD_PROJECT=[CLIENT-PRODUCTION-PROJECT-ID]
REGION=us-central1

# OAuth Configuration (Client-specific, create in their project)
# REQUIRED: Create OAuth client in client's GCP project
GOOGLE_CLIENT_ID=[CLIENT-PRODUCTION-OAUTH-CLIENT-ID]
GOOGLE_CLIENT_SECRET=[CLIENT-PRODUCTION-OAUTH-CLIENT-SECRET]

# Gemini AI (Client's key or yours)
GOOGLE_AI_API_KEY=[CLIENT-OR-YOUR-GEMINI-API-KEY]

# JWT Secret (unique for client production: openssl rand -base64 32)
JWT_SECRET=[GENERATE-NEW-JWT-SECRET-FOR-CLIENT-PRODUCTION]

# Service URL (update after first deployment)
PUBLIC_BASE_URL=https://flow.client-domain.com

# Custom Domain
CUSTOM_DOMAIN=flow.client-domain.com

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_DEBUGGING=false  # DISABLED in production
ENABLE_RATE_LIMITING=true

# Production-specific settings
MIN_INSTANCES=2
MAX_INSTANCES=50
MEMORY=2Gi
CPU=4
